Instructions: (Recommended to create venv for isolation and avoiding conflicts)

1. Install dependencies:
pip install -r requirements.txt
2. Update BMS and District URLs and Input Cities/States list.
3. Run the script

